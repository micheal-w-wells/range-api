name: Node CI
on:
  pull_request:
    branches:
    - PR_DEPLOY_DEV

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Get PR Info 
      env:
        PR_DEPLOY_FRIENDLY_SUFFIX: myra
    run: grep findThisVar2 $GITHUB_EVENT_PATH 
